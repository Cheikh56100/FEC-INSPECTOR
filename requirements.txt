# ─── FEC Audit Pro v5 — Requirements Streamlit ───────────────
# Installer avec : pip install -r requirements.txt

# Interface web
streamlit>=1.35.0

# Analyse de données
pandas>=2.0.0
numpy>=1.26.0

# Rapport Word (Module 1 & 3 — export .docx)
python-docx>=1.1.0

# API Claude — Rapport de Mission IA (Module 1)
anthropic>=0.28.0

# ─── Inclus dans la bibliothèque standard Python ──────────────
# json, sys, os, math, re, datetime, collections, argparse, warnings
# (pas besoin de les ajouter ici)
